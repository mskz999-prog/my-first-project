# CLAUDE.md

このファイルは、Claude Codeがこのリポジトリで作業する際の背景情報・進め方の指針です。
（ローカル/ターミナル版のClaude Codeで開いた場合もこのファイルが自動で読み込まれます。
 このリポジトリでの過去のやり取りの記憶はセッションごとにリセットされるため、
 引き継ぎ情報は基本的にこのファイルと`content_studio/`配下のコード・コメントに
 集約するようにしています。）

## プロジェクト概要（2プロジェクトが同居）

このリポジトリには現在2つのプロジェクトが同居しています。

1. **`content_studio/`（現在の本線）** — 古着の年代判別を教育コンテンツ化する
   Instagramカルーセル生成パイプライン。詳細は下記「content_studio: 年代判別
   コンテンツパイプライン」セクション参照。
2. **`src/`（お蔵入り・休止中）** — 古着せどり・バイヤー向けの市場データ自動収集＆
   レポート生成パイプライン。メルカリ・Yahoo!オークション・BASE・独立系ヴィンテージ
   古着店から相場データを収集し、Claude（Anthropic API）で「note有料級レポート」
   「Instagramカルーセル構成案」「リール台本」を生成する仕組み。**2026年9月時点で
   利用者の意向により一旦お蔵入り（休止）**。コード・データは削除せず残しているが、
   週次のGitHub Actions自動実行（`.github/workflows/weekly_report.yml`）は
   停止済み（`schedule`トリガーをコメントアウト、手動実行`workflow_dispatch`のみ
   残してある）。技術的な仕組みの詳細は`README.md`に網羅されているので、再開したく
   なった場合はそちらを参照。**新規の作業依頼が来たら、基本的にはcontent_studio側の
   話だと想定してよい**（せどりパイプライン側の話であれば依頼内容から明確にわかるはず）。

## 利用者について（重要）

- リポジトリのオーナーは**非エンジニア**。Python/YAMLはほぼ読めない・書けない。
- **古着が趣味で、将来的に副業・コンテンツ発信で収益化できたらいいな**という段階。
  現状は趣味〜検証フェーズであり、本格的な事業運用はまだ先。
- 上記の通り、せどり市場データパイプライン（`src/`）は休止中。今は`content_studio/`の
  古着年代判別コンテンツ（Instagramカルーセル）作りが本線。

## やり取りの進め方

- **やり取りは日本語で**。コミットメッセージなど英語が自然な箇所は英語のままでよい。
- コードやYAMLの中身をそのまま説明するのではなく、「何が変わるか」「なぜそれが必要か」を
  平易な日本語で要約してから作業する。専門用語（例: スクレイパー、正規化、トークン等）を
  使う場合は一言で補足する。
- 変更後は、diffを読ませる前提ではなく「結果として何がどう変わったか」を言葉で説明する。
- 事前に詳細な作業計画を提示することは必須ではないが、影響範囲が大きい変更
  （例: スクレイパーの収集頻度を上げる、自動投稿機能を追加する等）は一言確認してから進める。

## content_studio: 年代判別コンテンツパイプライン（現在の本線）

古着（現在は「VANS AUTHENTIC」のスニーカーが題材）の年代判別ポイントを、Instagram
カルーセル画像として教育コンテンツ化するパイプライン。流れは概ね次の通り。

1. **リサーチ** — 年代ごとの見分けポイント（ヒールパッチ／サイドタグ／ソール／インソール等）
   をWebSearchで複数ソースを横断調査。ファクトの採用基準は「一次情報の裏取りが理想だが、
   古着の年代判別はそこまで厳密に検証できないことも多いので、複数の古着専門ブログ等が
   **矛盾しない内容**を紹介していれば採用してよい」という緩めの基準（本人の明言）。
   伝承・逸話レベルの情報は「〜と伝えられている」等のヘッジ表現を使う。
2. **データ化** — `content_studio/data/vans_authentic_eras.mjs` に年代ごとのテキスト
   （`range`/`name`/`bullets`）と、メディア項目（`heelPatch`/`insole`/`sideTag`/
   `shoeSole`）を記述する。年代の刻み方・表記の細かさは、本人が手描きで作った参考チャート
   （年代×4項目の一覧表）を最優先の判断材料にする。
3. **画像素材** — `content_studio/assets/`配下（`heel_patches/`・`insoles/`・
   `side_tags/`・`soles/`・使わなかったものは`spares/`）に画像を格納し、各メディア項目の
   `photo`フィールドで参照する。**画像生成は本人が自分のGemini（有料アカウント）で
   手動生成し、GitHubにアップロードする運用**（Claude Code on the webのセッションは
   チャットに貼られた画像をファイルとして保存できず、一般サイトへのWebFetch/画像取得も
   環境のネットワークポリシーでブロックされることが多いため）。アップロード先はリポジトリ
   のどこでもよく、後でこちら側で整理する。**適切な写真が無い項目は`photo`を付けず、
   SVGイラスト（`lib/illustrations.mjs`）へのフォールバックに任せてよい**（無理に
   写真を当てはめない）。ローカル/ターミナル版のClaude Codeであれば、Webサイトから
   直接実物写真を取得できる可能性がある（ネットワーク制限が外れるため）。
4. **レンダリング** — `node content_studio/render_era_slides.mjs`でHTML/CSSを
   Playwright(Chromium)でスクリーンショットし、`content_studio/output/`に
   1080×1350pxのPNGカルーセル画像（表紙・概説＋年代ごとに1ページ、計8枚）を生成する。

**運用ルール**:
- 各年代ページは「ヒールパッチ・インソール・サイドタグ・アウトソール」の4項目を基本
  構成とする（登場前の要素が無い年代は代わりに関連する別項目を入れてよい）。
- 変更のたびに再レンダリングし、生成した画像を実際に見せて（`SendUserFile`等）本人の
  確認を経てからコミット・プッシュする。画像を直接見せずに「直しました」とだけ報告する
  運用は過去にNGだったフィードバックがある。

**技術メモ**:
- `render.mjs`/`render_era_slides.mjs`はPlaywrightを`npm install`後のローカル
  `node_modules`優先で解決し、無ければこのクラウド環境固有の固定パスにフォールバックする
  （`content_studio/package.json`に`playwright`を依存として記載済み）。ローカルでは
  `cd content_studio && npm install && npx playwright install chromium`だけで動く。
- `page.setContent()`は`file://`参照を読み込めないため、画像はbase64のdata URIとして
  埋め込んでいる（`render_era_slides.mjs`の`photoDataUri()`）。

## src/（休止中）についての補足

以下は`src/`（せどり市場データパイプライン）を将来再開する場合の参考情報。普段の作業では
基本的に読み飛ばしてよい。

- 各スクレイパーの `request_interval_sec` など対象サイトへの負荷を抑える設定は安易に
  緩めない（README「利用規約・コンプライアンス」参照）。
- SNS等への自動投稿機能は追加しない設計（人の最終判断を必須にする方針）。
- `trend`（実売データ）と`reference_benchmark`（在庫のみで売却時期不明なデータ）の
  集計分離、レポートの数値はサーバー側集計のみを信頼しLLMに数字を作らせない設計
  （`report_generator._quick_stats`）は、精度に関わるため崩さない。
- よくある作業: スクレイパー修正（`src/scrapers/`、特にMercariは壊れやすい）、
  `config/brand_catalog.yaml`へのブランド・モデル追加、`report_generator.py`/
  `system_prompt.md`の調整。

```bash
python -m src.main collect   # データ収集のみ
python -m src.main run       # 収集からレポート生成まで一気通貫
python -m src.main report --input data/raw/collected_XXXXXXXXTXXXXXXZ.jsonl  # 再生成のみ
pytest tests/                # ユニットテスト（ネットワークアクセスなし）
```
