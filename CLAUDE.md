# CLAUDE.md

このファイルは、Claude Codeがこのリポジトリで作業する際の背景情報・進め方の指針です。

## プロジェクト概要

古着せどり・バイヤー向けの市場データ自動収集＆レポート生成パイプライン。
メルカリ・Yahoo!オークション・BASE・独立系ヴィンテージ古着店から相場データを収集し、
Claude（Anthropic API）で「note有料級レポート」「Instagramカルーセル構成案」「リール台本」を
生成する。技術的な仕組みの詳細は `README.md` に網羅されているので、実装を変更する前に
関連セクションを確認すること。投稿の自動化は行わない設計（人が最終判断してnote/Instagramに
投稿する）。

## 利用者について（重要）

- リポジトリのオーナーは**非エンジニア**。Python/YAMLはほぼ読めない・書けない。
- 本人がせどりを実践しているわけではなく、**古着が趣味で、将来的に副業化できたらいいな**
  という段階。現状は趣味〜検証フェーズであり、本格的な事業運用はまだ先。
- そのため、週次のGitHub Actions自動実行（`.github/workflows/weekly_report.yml`）は
  現状も設定は残っているが、**今後は手動実行（`python -m src.main run`）を中心にしたい**
  という意向。自動実行を今後どうするか（このまま継続／一時停止）は本人がまだ決めかねている
  ため、こちらから強く誘導せず、話題に上ったら意向を確認すること。

## やり取りの進め方

- **やり取りは日本語で**。コミットメッセージなど英語が自然な箇所は英語のままでよい。
- コードやYAMLの中身をそのまま説明するのではなく、「何が変わるか」「なぜそれが必要か」を
  平易な日本語で要約してから作業する。専門用語（例: スクレイパー、正規化、トークン等）を
  使う場合は一言で補足する。
- 変更後は、diffを読ませる前提ではなく「結果として何がどう変わったか」を言葉で説明する。
- 事前に詳細な作業計画を提示することは必須ではないが、影響範囲が大きい変更
  （例: スクレイパーの収集頻度を上げる、自動投稿機能を追加する等）は一言確認してから進める。

## よく依頼される作業

1. **スクレイパーの修正** — Mercari/Yahoo/BASE/独立系ショップ側のサイト構造変化で
   収集が壊れた際の対応（`src/scrapers/`）。特にMercariはREADMEにある通り壊れやすい実装。
2. **`config/brand_catalog.yaml` へのブランド・モデル追加** — ブランド/型番カタログの拡充。
   YAMLの追記だけで完結する作業だが、本人はYAMLを直接編集できないため、依頼内容
   （ブランド名・代表モデル・カタカナ表記の有無等）をヒアリングした上でこちらで編集する。
3. **レポート生成ロジック・プロンプトの改善** — `src/pipeline/report_generator.py` や
   `src/prompts/system_prompt.md` の調整。
4. **週次レポートの内容チェック・コンテンツ企画への活用** — 生成された
   `data/reports/*.md` を一緒に確認し、note/Instagram投稿のネタ出しを手伝う。

## 変更時の注意点（絶対ルールではなく判断材料）

利用者からは「特にこだわりはない、都度判断してほしい」との回答だが、既存実装には以下の
設計意図があるため、変更時はこれらを壊していないか一応意識する（迷ったら確認する）。

- 各スクレイパーの `request_interval_sec` など、対象サイトへの負荷を抑える設定は
  安易に緩めない（README「利用規約・コンプライアンス」参照）。
- SNS等への自動投稿機能は現状追加しない設計（人の最終判断を必須にする方針）。
- `trend`（実売データ）と `reference_benchmark`（在庫のみで売却時期不明なデータ）の
  集計分離、およびレポートの数値はサーバー側集計のみを信頼しLLMに数字を作らせない設計
  （`report_generator._quick_stats`）は、精度に関わるため崩さない。

## コマンド（README「使い方」より）

```bash
python -m src.main collect   # データ収集のみ
python -m src.main run       # 収集からレポート生成まで一気通貫
python -m src.main report --input data/raw/collected_XXXXXXXXTXXXXXXZ.jsonl  # 再生成のみ
pytest tests/                # ユニットテスト（ネットワークアクセスなし）
```

Python/YAMLを直接読み書きしない前提の依頼が来ることが多いので、コマンド実行や
ファイル編集は基本的にこちらで代行する。
